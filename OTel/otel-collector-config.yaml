receivers:
  otlp:
    protocols:
      grpc:

exporters:
  otlp:
    endpoint: tempo:4317
    tls:
      insecure: true

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [otlp]

  # 👇 요 부분을 없애거나 아예 안 적기
  # pipelines:
  #   metrics:
  #     receivers: [otlp]
  #     exporters: [otlp]
